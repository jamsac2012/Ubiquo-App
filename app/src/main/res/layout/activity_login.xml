<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/login_hdpi_portrait"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context="la.applica.ubiquo.Login">


    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:hint="@string/tv_login_user"
        android:textColor="@color/primary_dark_color"
        android:background="@drawable/rounded_login_edittextcorners"
        android:ems="10"
        android:id="@+id/et_login_user"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="143dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:background="@drawable/rounded_login_edittextcorners"
        android:ems="10"
        android:textColor="@color/primary_dark_color"
        android:id="@+id/et_login_password"
        android:hint="@string/tv_login_pass"
        android:layout_marginTop="46dp"
        android:layout_alignTop="@+id/et_login_user"
        android:layout_alignLeft="@+id/et_login_user"
        android:layout_alignStart="@+id/et_login_user" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_login_button"
        android:textAllCaps="false"
        android:text="@string/bt_login"
        android:id="@+id/bt_login"
        android:onClick="login"
        android:layout_below="@+id/et_login_password"
        android:layout_marginTop="21dp"
        android:layout_alignRight="@+id/et_login_password"
        android:layout_alignEnd="@+id/et_login_password"
        android:layout_alignLeft="@+id/et_login_password"
        android:layout_alignStart="@+id/et_login_password"
        android:clickable="true" />
</RelativeLayout>
